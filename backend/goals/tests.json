{
  "tests": [
    {
      "name": "Get goal without auth",
      "method": "GET",
      "path": "/goal",
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    },
    {
      "name": "Get settings without auth",
      "method": "GET",
      "path": "/settings",
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}
